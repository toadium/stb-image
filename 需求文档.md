# 项目一：`stb-image`

### 目标用户
在 MoonBit 中处理图片资源的前端/游戏/工具开发者，以及希望用 MoonBit 快速读取 PNG/JPEG/BMP/GIF/WebP 等常见格式的个人开发者。

### 要解决的核心问题
MoonBit 当前已有 `mizchi/image` 等图像 codec 库（经核验 PNG/JPEG/BMP 有 decode+encode，GIF/WebP 仅 encode 无 decode），但缺少对广泛使用的 `stb_image.h` 的绑定。`stb_image` 是事实上的行业标准单头文件库，API 稳定、支持格式多（含 HDR/PSD/PIC 等 mizchi/image 未覆盖格式），且 mooncakes 全库检索确认 **stb 绑定完全空白**。

### MVP 范围
- 只暴露 load 路径：从文件路径、`Bytes`、`InputStream` 加载图片，返回 `Image` 值类型。当前 MVP 限定本地/内存入口，网络流读取可后续扩展。
- 支持格式：PNG、JPEG、BMP、GIF、WebP、TGA、PSD、HDR、PIC（与 stb_image 默认配置一致）。
- 返回图像信息：width、height、channels（灰度/RGB/RGBA）、data（`Bytes`）。
- 错误处理：以 MoonBit `Error` 返回“不支持的格式”或“解码失败”，不暴露 C 错误码。
-  vendoring：将 `stb_image.h` 的稳定版本放入 `native-stub`，提供可重复运行的下载脚本。脚本需固定版本号、校验哈希，失败时报错退出，不自动回退。

### FFI 方案要点（依据 moonbit-c-binding 技能）
- `ffi.mbt` 门控 `targets: {"ffi.mbt": ["native"]}`，`stb_image.h` 放入 `native-stub` 同目录。
- C wrapper 负责 ABI 归一化：`stbi_load_from_memory` 返回的 `unsigned char*` 拷贝到 `moonbit_make_bytes` 后由 MoonBit GC 接管，C 侧用 `stbi_image_free` 释放原始指针。
- 数据返回统一用 `Bytes`（避免 `FixedArray[Byte]` 二选一歧义），配合 `@utf8` 处理无需字符串转换（像素数据为二进制）。
- 所有权：输入 `Bytes` 用 `#borrow`（stb 仅在调用期间读取，不存储引用）。

### 验收标准
- `moon check` / `moon test --target native` 通过。
- 提供 6-10 张测试图片（PNG/JPEG/BMP/GIF/WebP）的自动回归测试，覆盖 happy path + 损坏文件 error path。
- 通过 `moonbit-c-binding` 的 ASan 验证脚本（`scripts/run-asan.py`），无内存泄漏/越界。
- `moon info --target native` 正常。
- 发布到 mooncakes.io 的包包含 `SKILL.md` 与 minimal example。

### 边界约束
- 不做 stb_image_write 绑定（MVP 仅读取）。
- 不做动态格式扩展钩子（MVP 只暴露默认配置）。
- 不追求零拷贝（MVP 允许解码后拷贝到 MoonBit `Bytes`）。
- 仅支持 native 目标；不暴露 wasm/js 构建。

