# 执行审查报告（v1 r3）

## 审查结果
REJECTED

## 发现

- **[一般]** v2.1 交付物清单遗漏 `src/bench.mbt`：功能项 1（第 228 行）"基准测试改用大图（4K+）后识别热点"明确要修改基准测试文件，测试与验证要求（第 242 行）也提到"基准测试：优化前后的性能对比基准（大图 4K+）"，但交付物清单（第 246-248 行）未列出 `src/bench.mbt`。这与 r2 已修正的 v2.0/v2.1 交付物清单遗漏问题同类（r2 修正了 v2.0 遗漏 ARCHITECTURE.md、v2.1 遗漏 image_chain.mbt 等），交付物清单应完整覆盖所有功能项的文件变更。

- **[轻微]** 跨版本风险表中 reexport 维护负担的缓解措施（第 321 行）引用"CI 添加校验（v1.19）"不够精确：v1.19 功能项 4 添加的是 `moon info` 校验（针对 `.mbti` 同步），并非 reexport 校验。`moon info` 可间接帮助发现 `.mbti` 失同步进而发现 reexport 问题，但不是直接的 reexport 校验，缓解措施表述易误导。

- **[轻微]** v2.1 交付物清单对功能项 2（并行化，第 229 行）涉及的 process 子包文件覆盖不够明确：功能项 2 标注"对热点算法尝试并行化"涉及 process 子包（L 工作量），但交付物清单仅列出 `src/process/frequency/fft.mbt`（功能项 3）和 `src/process/image_chain.mbt`（功能项 9），未明确功能项 2 若落地需修改的 process 文件。虽为条件性功能项（"评估...尝试"），但 L 工作量项应有交付物预判。

## 修改要求

1. **v2.1 交付物清单遗漏 `src/bench.mbt`**：
   - 问题：功能项 1 明确要"基准测试改用大图（4K+）"，测试要求也提到"基准测试：优化前后的性能对比基准（大图 4K+）"，但交付物清单未列出 `src/bench.mbt`，交付物清单与功能项/测试要求不一致。
   - 为什么是问题：交付物清单是版本交付的可追踪依据，遗漏功能项明确要修改的文件会导致交付验收时漏检，且与 r2 修正的同类问题标准不一致。
   - 期望修正方向：在 v2.1 交付物清单"修改"项补充 `src/bench.mbt`（基准测试改用大图 4K+ 并添加优化前后对比基准），与功能项 1 和测试要求一致。
