# 检查审查报告（v1 r1）

## 审查结果
APPROVED

## 发现

经独立读取 `src/pure/bmp_decode.mbt`、`src/pure/bmp_decode_test.mbt`、`src/pure/moon.pkg` 三个源文件，逐项核对检查报告中的行号引用与内容声明，确认全部准确：

- **行号引用核对**：`bmp_decode.mbt:8`（签名）、`:11-29`（文件头解析）、`:31-37`（位深/压缩分支）、`:45,58-62`（行序）、`:49`（行填充对齐公式）、`:67`（越界）、`:77-80`（32-bit Alpha 分支）均与实际源码一致；`bmp_decode_test.mbt:88-107`（对比测试）、`:110-132`（错误路径测试）亦一致。
- **失败路径计数核对**：源码中 `raise @core.LoadError::DecodeFailed(...)` 共 7 处（行 12/16/22/32/36/40/67），消息全中文，与检查报告声明一致。
- **测试数量核对**：测试文件含 8 个 `test` 块，与检查报告"8 个测试全部 passed"声明一致。
- **moon.pkg 配置核对**：`import @core` + `supported_targets = "native"`，与检查报告"与 src/format/moon.pkg 同构"声明一致。
- **对比测试字段核对**：测试 5-6 确实逐字段比较 width/height/channels/data 四字段（行 92-95、103-106）。

检查项覆盖了 task_v1.md 全部具体要求（目录结构、签名、24/32-bit 支持、文件头解析、行填充、行序、中文错误消息、5 类测试场景、构建验证、不破坏现有测试），方法可靠（命令实际执行、文件实际读取、行号可验证），PASSED 结论有充分证据支撑。

以下为值得改进的轻微问题，不影响结论：

- **[轻微]** 基准测试未显式验证：任务约束 6 提及"29 基准测试必须继续通过"，检查报告仅运行 `moon test --target native`（554 单元测试），未单独运行基准测试。本轮仅新增 pure 包未修改任何现有代码，基准测试不受影响，但严格而言应显式说明。
- **[轻微]** ASan 适用性未说明：任务约束 5 提及"ASan 验证（FFI 部分）"，pure 包为纯 MoonBit 实现无 FFI，ASan 不适用，检查报告未明确声明这一豁免理由。
- **[轻微]** API 冻结原则未直接验证：任务约束 1 要求"不修改已有签名"，检查报告未通过 git diff 等方式直接确认未修改已有文件，仅以全量测试通过间接保证。do_v1.md 产出清单仅列三个新建文件，间接可信。
- **[轻微]** 32-bit 对比测试缺失：对比测试（测试 5-6）仅覆盖 24-bit BMP 与 FFI 对照，未覆盖 32-bit 与 FFI 对照。任务要求未明确要求覆盖所有位深，但作为覆盖度改进点值得后续关注。
- **[轻微]** 错误路径测试覆盖 2/7：7 条失败路径仅测试 invalid magic 与 too short 两条，不支持压缩/位深/尺寸无效/越界等路径未单独测试。任务具体测试列表未要求全覆盖，但作为健壮性改进点值得后续关注。
