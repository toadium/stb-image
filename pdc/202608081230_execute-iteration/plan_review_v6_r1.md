# 计划审查报告（v6 r1）

## 审查结果
REJECTED

## 发现
- **[一般] 错误路径测试覆盖与实现要求不一致**：task_v6.md line 16 明确要求 pure 解码器实现三种错误路径（"数据过短、不支持的 image type、不支持的 bpp → raise @types.LoadError::DecodeFailed(...)"），但 line 26 测试覆盖建议仅明确列出两种（"错误路径：数据过短、不支持的 image type（如 type 1 颜色映射）"），遗漏了"不支持的 bpp"错误路径测试。line 16 与 line 26 同一文档内对错误路径枚举不一致，Doer 按 line 26 的"建议覆盖"列表实现时可能遗漏"不支持的 bpp"测试，导致 pure 解码器在 bpp=8/16 等不支持位深时的错误处理分支未被验证。该分支虽实现简单（`if bpp != 24 && bpp != 32`），但 TGA 格式存在多种位深（8/15/16 灰度/索引），错误拒绝逻辑是解码器健壮性的关键组成，未测试则正确性无保证。对比 BMP 解码器先例（`src/pure/bmp_decode.mbt:31` 同样有 `bpp != 24 && bpp != 32` 拒绝分支），BMP 测试虽也未覆盖该分支，但 BMP 的 task_v1 未明确列举三种错误路径，而 task_v6.md line 16 已明确列举三种，line 26 应与之一致。

## 修改要求
1. **问题**：task_v6.md line 26 测试覆盖建议的错误路径枚举（数据过短、不支持的 image type）遗漏了 line 16 已明确要求的"不支持的 bpp"错误路径。
   **为什么是问题**：line 16 是实现要求（三种错误路径），line 26 是测试覆盖指导（只列两种），同一文档内不一致，Doer 按 line 26 实现会遗漏"不支持的 bpp"错误路径测试，该分支未被验证。
   **期望的修正方向**：在 task_v6.md line 26 的错误路径测试覆盖建议中补充"不支持的 bpp（如 bpp=16 或 bpp=8）"一项，与 line 16 的三种错误路径一致。相应地，测试数建议可从"7-9 个"调整为"8-10 个"以容纳新增的错误路径测试，或保持"7-9 个"但明确其中 3 个为错误路径测试（数据过短 + 不支持 image type + 不支持 bpp）。预期 native 测试数同步调整（562 + N_pure_tga + 1，N_pure_tga 含 3 个错误路径测试）。
